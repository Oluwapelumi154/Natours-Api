const {
  createTour,
  deleteTour,
  getAllTours,
  getTour,
  updateTour
} = require('./tour-controller');

module.exports = Object.freeze({
  createTour,
  deleteTour,
  getAllTours,
  getTour,
  updateTour
});
